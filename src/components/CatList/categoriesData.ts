import { ICategoryList } from "../../types/CommonTypes"

export const categoriesData: ICategoryList = {
    id: Math.floor(Math.random() * 1000000000),
    body: 'Categories',
    level: 0,
    subCategories: [],
}